/* Portfolio bars are HTML in portfolio.html.
   A canvas chart went blank on every refresh: the poll replaces <main>,
   which threw away the drawn canvas, and canvas fill cannot paint a CSS
   variable. This file is intentionally empty so an older cached page that
   still requests it does not throw. */
